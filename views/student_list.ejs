<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>รายชื่อ</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Sarabun:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800&display=swap" rel="stylesheet">
  
    <style>
        body {
            font-family: "Sarabun", sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            color: #333;
            line-height: 1.6;
        }

        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h1 {
            font-size: 2rem;
            margin-bottom: 20px;
            color: #222;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            font-size: 1rem;
        }

        th, td {
            padding: 12px;
            border-bottom: 1px solid #ddd;
            text-align: left;
        }

        th {
            background-color: #f4f4f4;
            font-weight: 600;
            color: #444;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        tr:hover {
            background-color: #f1f1f1;
        }

        a {
            display: inline-block;
            margin-top: 20px;
            color: #007bff;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        button {
            padding: 10px 20px;
            font-size: 16px;
            background-color: #28a745;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 20px;
        }

        button:hover {
            background-color: #218838;
        }
    </style>

</head>
<body>
    <div class="container">
        <h1>รายชื่อนักศึกษา</h1>

        <table>
            <thead>
                <tr>
                    <th>Student ID</th>
                    <th>Section ID</th>
                    <th>Active Date</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Curriculum</th>
                </tr>
            </thead>
            <tbody>  

            <!--ส่วนนี้ใช้เทมเพลต EJS เพื่อวนลูปแสดงข้อมูลจาก attendanceRecords ซึ่งถูกดึงมาจากฐานข้อมูลแล้วส่งมายังหน้าเว็บ -->
                <% attendanceRecords.forEach(record => { %> 
                    <tr>
                        <td><%= record.student_id %></td>
                        <td><%= record.section_id %></td>
                        <td><%= record.active_date %></td>
                        <td><%= record.first_name %></td>
                        <td><%= record.last_name %></td>
                        <td><%= record.short_name_en %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>

        <a href="/">Go back to Welcome Page</a>
    </div>
</body>
</html>
